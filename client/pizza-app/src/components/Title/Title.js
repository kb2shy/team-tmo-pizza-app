import React from 'react'
import PropTypes from 'prop-types'

const Title = props => {
    return (
        <div>
            {props.title}
        </div>
    )
}

Title.propTypes = {

}

export default Title
