// Better to store this constant in separate JSON config file and not commit,
// but for the sake of the demo...
const AUTH_JWT_SECRET = 'MY_PREFERRED_AUTH_SECRET';

module.exports = {
  AUTH_JWT_SECRET,
};
